[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
python_files = test_*.py
addopts = -q --disable-warnings --strict-markers
markers =
    postgres: requires the isolated PostgreSQL 18 integration database
