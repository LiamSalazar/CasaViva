services:
  caddy:
    ports: !override
      - "127.0.0.1:8443:443"
    volumes:
      - "./docker/Caddyfile.bootstrap:/etc/caddy/Caddyfile:ro"
      - "caddy_data:/data"
      - "caddy_config:/config"
